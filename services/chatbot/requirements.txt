# ── LLM 클라이언트 (vLLM / Ollama OpenAI 호환) ──
openai>=1.30.0
ollama>=0.2.0

# ── 임베딩 (KR-SBERT 768차원) ──
sentence-transformers>=3.0.0
torch>=2.0.0                  # sentence-transformers 의존성

# ── Vector DB ──
qdrant-client>=1.9.0

# ── PostgreSQL (async) ──
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0

# ── FastAPI 서버 ──
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0      # BaseSettings (.env 로딩)

# ── 유틸 ──
python-dotenv>=1.0.0
httpx>=0.27.0
aiosqlite>=0.20.0
# ── 모니터링 및 LangChain (RAG 관측) ──
langsmith>=0.7.17
langchain>=1.2.12
langchain-openai>=1.1.11
langgraph>=1.1.2
