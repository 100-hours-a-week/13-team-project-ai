name: AI RECOMMEND V2 CD Pipeline
run-name: AI RECOMMEND V2 CD • sha-${{ github.event.workflow_run.head_sha }}

on:
  workflow_run:
    workflows: ["AI RECOMMEND V2 CI Pipeline"]
    types: [completed]

permissions:
  id-token: write
  actions: read

concurrency:
  group: recommend-v2-cd-${{ github.event.workflow_run.head_sha }}
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' }}
    runs-on: ubuntu-24.04-arm
    environment: ${{ github.event.workflow_run.head_branch == 'main' && 'production' || 'develop' }}
    env:
      AWS_REGION: ap-northeast-2
      IMAGE_TAG: sha-${{ github.event.workflow_run.head_sha }}
      COMPOSE_DIR: /home/ubuntu/recommend
      DEPLOY_ENV: ${{ github.event.workflow_run.head_branch == 'main' && 'prod' || 'dev' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync config to S3
        run: aws s3 sync services/recommend/docker/ s3://moyeobab-${{ env.DEPLOY_ENV }}-config/recommend/ --delete

      - name: Deploy via SSM Send Command
        id: ssm-deploy
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --targets "Key=tag:Service,Values=recommend" "Key=tag:Environment,Values=${{ env.DEPLOY_ENV }}" \
            --document-name "AWS-RunShellScript" \
            --comment "Deploy recommend ${{ env.IMAGE_TAG }}" \
            --timeout-seconds 300 \
            --max-concurrency "1" \
            --max-errors "0" \
            --parameters commands='[
              "set -eu",
              "REGION='"${{ env.AWS_REGION }}"'",
              "IMAGE_TAG='"${{ env.IMAGE_TAG }}"'",
              "COMPOSE_DIR='"${{ env.COMPOSE_DIR }}"'",
              "DEPLOY_ENV='"${{ env.DEPLOY_ENV }}"'",
              "",
              "# S3에서 배포 설정 파일 동기화 (docker-compose, promtail)",
              "mkdir -p ${COMPOSE_DIR}",
              "aws s3 sync s3://moyeobab-${DEPLOY_ENV}-config/recommend/ ${COMPOSE_DIR}/ --exclude '.env' --region ${REGION}",
              "",
              "# 현재 이미지 태그 백업 (롤백용)",
              "PREV_IMAGE=$(docker inspect --format={{.Config.Image}} $(docker compose -f ${COMPOSE_DIR}/docker-compose.yml ps -q app) 2>/dev/null || echo \"\")",
              "echo \"[INFO] Current image: ${PREV_IMAGE}\"",
              "echo \"[INFO] Target image tag: ${IMAGE_TAG}\"",
              "",
              "# ECR login",
              "ECR_REGISTRY=$(aws sts get-caller-identity --query Account --output text).dkr.ecr.${REGION}.amazonaws.com",
              "aws ecr get-login-password --region ${REGION} | docker login --username AWS --password-stdin ${ECR_REGISTRY}",
              "EXPECTED_IMAGE=${ECR_REGISTRY}/moyeobab/${DEPLOY_ENV}/recommend:${IMAGE_TAG}",
              "echo \"[INFO] Expected image: ${EXPECTED_IMAGE}\"",
              "",
              "# Parameter Store → .env",
              "> ${COMPOSE_DIR}/.env",
              "for param in PG_HOST PG_PORT PG_USER PG_PASSWORD PG_DB QDRANT_URL QDRANT_API_KEY QDRANT_COLLECTION; do",
              "  val=$(aws ssm get-parameter --name /moyeobab/recommend/${DEPLOY_ENV}/${param} --with-decryption --query \"Parameter.Value\" --output text --region ${REGION})",
              "  echo \"${param}=${val}\" >> ${COMPOSE_DIR}/.env",
              "done",
              "echo \"ECR_REGISTRY=${ECR_REGISTRY}\" >> ${COMPOSE_DIR}/.env",
              "echo \"IMAGE_TAG=${IMAGE_TAG}\" >> ${COMPOSE_DIR}/.env",
              "echo \"ENV=${DEPLOY_ENV}\" >> ${COMPOSE_DIR}/.env",
              "",
              "# Deploy (app 서비스만 명시적으로 pull + recreate)",
              "cd ${COMPOSE_DIR}",
              "docker compose pull app",
              "docker compose up -d --force-recreate --no-deps app",
              "docker compose up -d",
              "",
              "# 이미지 태그 검증 (컨테이너가 기대한 이미지로 실행 중인지 확인)",
              "sleep 3",
              "RUNNING_IMAGE=$(docker inspect --format={{.Config.Image}} $(docker compose ps -q app) 2>/dev/null || echo \"\")",
              "echo \"[INFO] Running image: ${RUNNING_IMAGE}\"",
              "if [ \"${RUNNING_IMAGE}\" != \"${EXPECTED_IMAGE}\" ]; then",
              "  echo \"[FAIL] Image mismatch! Expected: ${EXPECTED_IMAGE}, Running: ${RUNNING_IMAGE}\"",
              "  echo \"[FAIL] docker compose pull 또는 up이 새 이미지를 반영하지 못했습니다.\"",
              "  # 롤백",
              "  if [ -n \"${PREV_IMAGE}\" ]; then",
              "    echo \"[ROLLBACK] Reverting to ${PREV_IMAGE}\"",
              "    PREV_TAG=${PREV_IMAGE##*:}",
              "    sed -i \"s/IMAGE_TAG=.*/IMAGE_TAG=${PREV_TAG}/\" ${COMPOSE_DIR}/.env",
              "    docker compose up -d --force-recreate --no-deps app",
              "    sleep 3",
              "    curl -sf http://localhost:8000/health && echo \"[ROLLBACK] Health check passed\" || echo \"[ROLLBACK] Health check also failed\"",
              "  fi",
              "  exit 1",
              "fi",
              "",
              "# Health check (최대 2분: 40회 x 3초)",
              "echo \"[INFO] Image verified, starting health check...\"",
              "HEALTH_OK=false",
              "for i in $(seq 1 40); do",
              "  if curl -sf http://localhost:8000/health; then",
              "    echo \"[OK] Health check passed (attempt ${i}/40)\"",
              "    HEALTH_OK=true",
              "    break",
              "  fi",
              "  echo \"[WAIT] Health check attempt ${i}/40 failed, retrying in 3s...\"",
              "  sleep 3",
              "done",
              "",
              "if [ \"${HEALTH_OK}\" = \"true\" ]; then",
              "  docker image prune -f",
              "  echo \"[SUCCESS] Deploy complete: ${EXPECTED_IMAGE}\"",
              "  exit 0",
              "fi",
              "",
              "# Rollback on health check failure",
              "echo \"[FAIL] Health check failed after 40 attempts\"",
              "if [ -n \"${PREV_IMAGE}\" ]; then",
              "  echo \"[ROLLBACK] Reverting to ${PREV_IMAGE}\"",
              "  PREV_TAG=${PREV_IMAGE##*:}",
              "  sed -i \"s/IMAGE_TAG=.*/IMAGE_TAG=${PREV_TAG}/\" ${COMPOSE_DIR}/.env",
              "  docker compose up -d app",
              "  sleep 5",
              "  for i in $(seq 1 20); do",
              "    if curl -sf http://localhost:8000/health; then",
              "      echo \"[ROLLBACK] Health check passed (attempt ${i}/20)\"",
              "      break",
              "    fi",
              "    echo \"[ROLLBACK] Health check attempt ${i}/20 failed, retrying in 3s...\"",
              "    sleep 3",
              "  done",
              "  curl -sf http://localhost:8000/health && echo \"[ROLLBACK] Succeeded\" || echo \"[ROLLBACK] Also failed\"",
              "fi",
              "exit 1"
            ]' \
            --query "Command.CommandId" --output text)

          echo "command_id=$COMMAND_ID" >> "$GITHUB_OUTPUT"
          echo "Command ID: $COMMAND_ID"

      - name: Wait for deployment result
        id: wait-result
        run: |
          COMMAND_ID="${{ steps.ssm-deploy.outputs.command_id }}"
          echo "Command ID: $COMMAND_ID"

          # SSM 타겟 인스턴스 ID 조회 (최대 30초 대기)
          INSTANCE_IDS=""
          for attempt in $(seq 1 6); do
            sleep 5
            INSTANCE_IDS=$(aws ssm list-command-invocations \
              --command-id "$COMMAND_ID" \
              --query "CommandInvocations[*].InstanceId" \
              --output text)
            if [ -n "$INSTANCE_IDS" ]; then
              break
            fi
            echo "Waiting for SSM targets... (attempt $attempt/6)"
          done

          # 타겟 인스턴스 0대 체크
          if [ -z "$INSTANCE_IDS" ]; then
            echo "::error::SSM 타겟 인스턴스가 없습니다. tag:Service=recommend, tag:Environment=${{ env.DEPLOY_ENV }} 에 매칭되는 인스턴스를 확인하세요."
            echo "result=no-targets" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          INSTANCE_COUNT=$(echo "$INSTANCE_IDS" | wc -w | tr -d ' ')
          echo "Target instances ($INSTANCE_COUNT): $INSTANCE_IDS"

          ALL_SUCCESS=true
          for INSTANCE_ID in $INSTANCE_IDS; do
            echo "--- Waiting for $INSTANCE_ID ---"
            for i in $(seq 1 60); do
              STATUS=$(aws ssm get-command-invocation \
                --command-id "$COMMAND_ID" \
                --instance-id "$INSTANCE_ID" \
                --query "Status" --output text 2>/dev/null || echo "Pending")

              if [ "$STATUS" = "Success" ]; then
                echo "$INSTANCE_ID: Success"
                # 성공 시에도 stdout 출력 (이미지 검증 결과 확인용)
                aws ssm get-command-invocation \
                  --command-id "$COMMAND_ID" \
                  --instance-id "$INSTANCE_ID" \
                  --query "StandardOutputContent" --output text 2>/dev/null | tail -5 || true
                break
              elif [ "$STATUS" = "Failed" ] || [ "$STATUS" = "Cancelled" ] || [ "$STATUS" = "TimedOut" ]; then
                echo "::error::$INSTANCE_ID: $STATUS"
                echo "--- stdout ---"
                aws ssm get-command-invocation \
                  --command-id "$COMMAND_ID" \
                  --instance-id "$INSTANCE_ID" \
                  --query "StandardOutputContent" --output text 2>/dev/null | tail -20 || true
                echo "--- stderr ---"
                aws ssm get-command-invocation \
                  --command-id "$COMMAND_ID" \
                  --instance-id "$INSTANCE_ID" \
                  --query "StandardErrorContent" --output text || true
                ALL_SUCCESS=false
                break
              fi
              sleep 5
            done
          done

          if [ "$ALL_SUCCESS" = "true" ]; then
            echo "result=success" >> "$GITHUB_OUTPUT"
          else
            echo "result=failure" >> "$GITHUB_OUTPUT"
            exit 1
          fi

      - name: Save IMAGE_TAG to Parameter Store
        run: |
          aws ssm put-parameter \
            --name "/moyeobab/recommend/${{ env.DEPLOY_ENV }}/IMAGE_TAG" \
            --value "${{ env.IMAGE_TAG }}" \
            --type String \
            --overwrite

      - name: Notify Discord
        if: always()
        env:
          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_CD }}
          DEPLOY_ENV: ${{ env.DEPLOY_ENV }}
          BRANCH: ${{ github.event.workflow_run.head_branch }}
          AUTHOR: ${{ github.event.workflow_run.head_commit.author.name }}
          COMMIT_MSG: ${{ github.event.workflow_run.head_commit.message }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          ENV_UPPER=$(echo "$DEPLOY_ENV" | tr '[:lower:]' '[:upper:]')
          RESULT="${{ steps.wait-result.outputs.result }}"

          if [ "${{ job.status }}" = "success" ]; then
            TITLE="[${ENV_UPPER}] AI Recommend 배포 성공"
            DESC="EC2에 \`${{ env.IMAGE_TAG }}\` 배포 완료"
            COLOR=3066993
          else
            TITLE="[${ENV_UPPER}] AI Recommend 배포 실패"
            if [ "$RESULT" = "no-targets" ]; then
              DESC="SSM 타겟 인스턴스가 없습니다 (tag:Service=recommend, tag:Environment=${DEPLOY_ENV})"
            elif [ "$RESULT" = "failure" ]; then
              DESC="EC2 배포 스크립트 실패 — 이전 버전으로 롤백 시도됨. Actions 로그에서 롤백 결과를 확인하세요"
            else
              DESC="배포 실패 — Actions 로그를 확인하세요"
            fi
            COLOR=15158332
          fi

          jq -n \
            --arg title "$TITLE" \
            --arg desc "$DESC" \
            --arg color "$COLOR" \
            --arg env_label "$ENV_UPPER" \
            --arg branch "$BRANCH" \
            --arg author "$AUTHOR" \
            --arg msg "$COMMIT_MSG" \
            --arg tag "${{ env.IMAGE_TAG }}" \
            --arg url "$RUN_URL" \
            '{
              embeds: [{
                title: $title,
                description: $desc,
                color: ($color | tonumber),
                fields: [
                  {name: "환경", value: $env_label, inline: true},
                  {name: "브랜치", value: $branch, inline: true},
                  {name: "작성자", value: $author, inline: true},
                  {name: "버전", value: ("`" + $tag + "`"), inline: true},
                  {name: "커밋", value: $msg, inline: false},
                  {name: "상세", value: ("[Actions 로그](" + $url + ")"), inline: false}
                ],
                footer: {
                  text: "AI Recommend CD"
                }
              }]
            }' | curl -sf -X POST "$WEBHOOK" -H "Content-Type: application/json" -d @-
